# Git canonize note

**Status:** public tip is published separately at [lk-bloom/hive](https://github.com/lk-bloom/hive).  
**Live registries:** keep under `registry/private/` on Admin machines — never on the public tip.  
See [`PUBLICATION.md`](../PUBLICATION.md).
